{
    "name": "@dad-chatbot/objective-backend",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "main": "dist/index.js",
    "types": "src/index.ts",
    "scripts": {
        "build": "tsc -p tsconfig.build.json",
        "clean": "rm -rf dist",
        "start": "node dist/index.js",
        "test": "jest --runInBand",
        "typecheck": "tsc --noEmit",
        "lint": "eslint . --ext .ts"
    },
    "dependencies": {
        "@dad-chatbot/objective-safety": "0.0.0",
        "@dad-chatbot/objective-schemas": "0.0.0"
    },
    "devDependencies": {}
}
