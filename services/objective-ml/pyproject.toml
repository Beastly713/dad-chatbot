[project]
name = "objective-ml"
version = "0.0.0"
description = "Standalone bounded ML service scaffold for objective physiological monitoring"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
